export default class CharacterMatrix {
  width : number;
  height : number;

  matrix : string[][];

  constructor(charMatrix : string[][])
  constructor(charMatrix : CharacterMatrix)
  constructor(charMatrix : any) {
    
  }
}